# ui

React app responsibilities:
- Upload PDFs and create jobs.
- Render previews and correction tools.
- Show job states/errors and rule history.

## Inputs/outputs
- Consumes backend API at `http://localhost:8080/v1`.
- Payload contracts from `../contracts/json-schemas`.
